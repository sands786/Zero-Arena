# Server Configuration (optional - defaults work for most cases)
# Uncomment and modify these if you need custom configuration

# VITE_SERVER_HOST=localhost
# VITE_SERVER_PORT=8000
# VITE_USE_WSS=false

# Examples for different deployment scenarios:

# Local development (default):
# VITE_SERVER_HOST=localhost
# VITE_SERVER_PORT=8000
# VITE_USE_WSS=false

# LAN play (same network):
# VITE_SERVER_HOST=192.168.1.100  # Replace with your server's local IP
# VITE_SERVER_PORT=8000
# VITE_USE_WSS=false

# Internet play (public server):
# VITE_SERVER_HOST=your-server-domain.com  # Or public IP address
# VITE_SERVER_PORT=8000
# VITE_USE_WSS=true  # Use secure WebSocket for HTTPS sites

# Production deployment with SSL:
# VITE_SERVER_HOST=game.yourdomain.com
# VITE_SERVER_PORT=443
# VITE_USE_WSS=true
